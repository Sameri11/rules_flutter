module(
    name = "namespace_dynamic_test",
    version = "0.0.1",
)

bazel_dep(name = "sameri11_rules_flutter", version = "0.1.0", repo_name = "rules_flutter")
bazel_dep(name = "bazel_skylib", version = "1.8.2")
bazel_dep(name = "platforms", version = "1.0.0")
bazel_dep(name = "rules_android", version = "0.7.3")
bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "rules_jvm_external", version = "7.1")

android_ndk = use_extension("@rules_flutter//flutter:extensions.bzl", "android_ndk")
use_repo(android_ndk, "androidndk", "androidndk_cmake")

local_path_override(
    module_name = "sameri11_rules_flutter",
    path = "../..",
)

dynamic_namespace_metadata = use_repo_rule("//:fixture.bzl", "dynamic_namespace_metadata")

dynamic_namespace_metadata(
    name = "dynamic_namespace_metadata",
    marker = "//:fixtures/namespace_dynamic/pubspec.yaml",
)

plugins = use_extension("@rules_flutter//flutter:extensions.bzl", "flutter_plugins_ext")
plugins.project(
    abis = ["arm64-v8a"],
    embedding = "//:dynamic_embedding",
    maven_repo = "//:dynamic_maven",
    metadata = "@dynamic_namespace_metadata//:.flutter-plugins-dependencies",
    package_config = "@dynamic_namespace_metadata//:package_config.json",
)
use_repo(plugins, "flutter_plugins")
